# ── Render service ────────────────────────────────────────────────────────
PORT=3001
# Shared secret — must match the RENDER_TOKEN Wrangler secret
RENDER_TOKEN=

# ── Cloudflare R2 (S3-compatible) ─────────────────────────────────────────
# Create an R2 API token at:
#   dash.cloudflare.com → R2 Object Storage → Manage R2 API Tokens
#   → Create API Token → Object Read & Write on bible-story-artifacts
R2_ACCOUNT_ID=fd5b09ffec9792936392a2fdeddf8590
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
